<p><%= t("devise.mailer.greeting", email: @email) %></p>
<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("devise.mailer.email_changed.pending", email: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t("devise.mailer.email_changed.changed", email: @resource.email) %></p>
<% end %>
